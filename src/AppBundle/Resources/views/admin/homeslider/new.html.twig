{% extends '@AppBundle/Resources/views/admin/form.html.twig' %}

{% block body %}
    <h1>Homeslider creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div  class="btn-group btn-group-circle btn-group-solid">
        <a href="{{ path('homeslider_index') }}" class="btn green">Back to the list</a>
        <input type="submit" value="Create" class="btn red"/>
    </div>
    {{ form_end(form) }}




{% endblock %}
