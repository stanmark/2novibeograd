{% extends '@AppBundle/Resources/views/admin/table.html.twig' %}

{% block thead %}
    <h1>Ourservices list</h1>
    <table class="table table-striped table-bordered table-hover js-table" id="sample_1"  >
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>Icon</th>
                <th>Actions</th>
            </tr>
        </thead>
    {% endblock %}

    {% block tbody %}
        <tbody>
            {% for ourService in ourServices %}
                <tr>
                    <td><a href="{{ path('ourservices_show', { 'slug': ourService.slug }) }}">{{ ourService.slug }}</a></td>
                    <td>{{ ourService.title }}</td>
                    <td>{{ ourService.description }}</td>
                    <td>{{ ourService.icon }}</td>
                    <td class="btn-group btn-group-circle btn-group-solid">
                        <a href="{{ path('ourservices_show', { 'slug': ourService.slug }) }}" class="btn blue">show</a>

                        <a href="{{ path('ourservices_edit', { 'slug': ourService.slug }) }}" class="btn yellow">edit</a>                  
                        <a class="btn red 
                           js-delete-row"  
                           href="#"                          
                           data-url="{{ path ('ourservices_delete', {'slug':ourService.slug}) }}"
                           >delete</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ourservices_new') }}" class="btn btn-circle green">Create a new ourService</a>

{% endblock %}

