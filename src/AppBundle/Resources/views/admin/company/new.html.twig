{% extends '@AppBundle/Resources/views/admin/form.html.twig' %}

{% block form %}
   

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn blue" />
    {{ form_end(form) }}

            <a href="{{ path('company_index') }} "class="btn default">Nazad</a>
  
{% endblock %}
