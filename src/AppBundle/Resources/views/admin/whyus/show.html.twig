{% extends '@AppBundle/Resources/views/admin/table.html.twig' %}

{% block tbody %}
    <h1>Whyu</h1>

    <table  class="table table-striped table-bordered table-hover" id="sample_1">
        <tbody>
            <tr>
                <th>Title</th>
                <td>{{ whyU.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ whyU.description |raw }}</td>
            </tr>           
        </tbody>
    </table>



    {{ form_start(delete_form) }}
    <div  class="btn-group btn-group-circle btn-group-solid">
        <a href="{{ path('whyus_index') }}" class="btn green">Back to the list</a>

        <a href="{{ path('whyus_edit', { 'id': whyU.id }) }}" class="btn yellow">Edit</a>
    </div>
    {{ form_end(delete_form) }}

{% endblock %}
