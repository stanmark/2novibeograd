{% extends '@AppBundle/Resources/views/admin/table.html.twig' %}

{% block thead %}
    <h1>SEO list</h1>
    <table class="table table-striped table-bordered table-hover js-table" id="sample_1"  >
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Titletag</th>
                <th>Title</th>
                <th>Actions</th>
            </tr>
        </thead>
    {% endblock %}

    {% block tbody %}
        <tbody>
            {% for sEO in sEOs %}              
                <tr>
                    <td><a href="{{ path('seo_show', { 'id': sEO.id }) }}">{{ sEO.id }}</a></td>
                    <td>{{ sEO.description }}</td>
                    <td>{{ sEO.titletag }}</td>
                    <td>{{ sEO.title }}</td>
                    <td class="btn-group btn-group-circle btn-group-solid">  
                        <a href="{{ path('seo_show', { 'id': sEO.id }) }}" class="btn blue">show</a>
                        <a href="{{ path('seo_edit', { 'id': sEO.id }) }}" class="btn yellow">edit</a>  
                        <a class="btn red 
                           js-delete-row"  
                           href="#"                          
                           data-url="{{ path ('company_delete', {'id':company.id}) }}"
                           >delete</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('seo_new') }}"class="btn btn-circle green">Create a new sEO</a>

{% endblock %}
{% block javascript %}   
    {{ parent() }}
    <script>
        $(document).ready(function () {
            var $table = $('.js-table');
            DeleteRow.initialize($table);

        });
    </script>
{% endblock %}
