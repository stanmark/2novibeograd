{% extends '@AppBundle/Resources/views/admin/form.html.twig' %}

{% block form %}
    <h1>Feature creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

            <a href="{{ path('features_index') }}">Back to the list</a>
  
{% endblock %}
